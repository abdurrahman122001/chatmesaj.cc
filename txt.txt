docker run --name chatbot-pg -e POSTGRES_PASSWORD=postgres -e POSTGRES_DB=chatbot -p 5432:5432 -d postgres:16

sudo apt install postgresql postgresql-contrib
sudo -u postgres psql
CREATE DATABASE chatbot;
CREATE USER chatbot WITH ENCRYPTED PASSWORD 'yourpass';
GRANT ALL PRIVILEGES ON DATABASE chatbot TO chatbot; 

cd server
cp .env.example .env
# .env-də DATABASE_URL və JWT_SECRET dəyərlərini təyin edin
npm install
npm run prisma:migrate
npm run seed        # admin@example.com / admin1234
npm run dev


cp .env.example .env   # əgər yoxdursa
# .env-də VITE_API_URL=http://localhost:4000 olsun
npm run dev